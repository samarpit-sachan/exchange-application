{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch Program",
            "skipFiles": ["<node_internals>/**"],
            "program": "${workspaceFolder}/api/src/index.ts", // Adjust the path to your entry file
            "outFiles": ["${workspaceFolder}/dist/**/*.js"], // Adjust the path to your compiled files
            "sourceMaps": true
        }
    ]
}